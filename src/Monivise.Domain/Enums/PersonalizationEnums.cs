using System;
using System.Collections.Generic;
using System.Text;

namespace Monivise.Domain.Enums
{
    public enum ExpenseCategory { Need, Want, Investment }
    public enum ItemNature { HardFixed, Soft, Unpriced }
    public enum PathwayType { Prudent, Moderate, Comfortable }
    public enum IncomeType { Primary, Extra }
    public enum GoalStatus { Active, Completed, Paused }
}
